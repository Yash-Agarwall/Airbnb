Setting up:
npm init -y
npm i express
npm i ejs
npm i mongoose

then we started off with the basic setup of app,js
connecting mongoose express localhost etc

Phase1(10 march): 

Model 1: Listing(place- aparetment, flat, house etc)
title
description
image
price
location
country

create models folder
create listing.js-> model and listing


in listing we are defining the schema, what is the type and other
props of a particular entry

then we used ternary operator to set the value of image urlf

then in app.js, we made the get request for /testListing
make sure you do module.exports, no typo

use mongosh, then you will see that wanderlust db is made
//i stopped here

now we are going to initialize the database
make init folder, then make data.js and copy paste the data from apna college website

now we made index.js, there we did basic setup like 
require mongoose, data.js, listing.js, mongourl, then main try catch

then we made async function main to connect mongo url
in that async function we first deleted the old sata(if any)
then we inserted the initData.data value, then we called the initDB function

now do nodemon index.js, then mongosh and use wanderlust, then do 
db.listings.find()

NOW WE WILL START WITH CRUD OPERATIONS

index route: get-> /listings-> all listings
come to app.js: make et request and get data form db, since we want to render it in ejs
so it will be better to use await asnyc to avoid callback hell of then catch
to render ejs we need to require it also

now we made a listings folder in views and then index.ejs inside it
now require view engine in app.js and set the path to s=render this index.ejs

now comes the important step.........we showed the data of allListings in the index.ejs
using for loop and anchor tag, each link will lead to a specific page


2nd route: show route
get-> listings/:id

so in show.ejs we have passed the data of a particular listing by using findByID
and we are displaying that in show.ejs, this route is rendered in app.js

now we want to display the price in indian standard format
1- use the hex code of rupee entity
2- use .toLocaleString("en-IN");

3rd route: new (this gives a form to create a new listing)
4th: create(this will let us create a new listing)

so first we are working on the new route
make the new.ejs file and do basic form setup, then render this in app.js
-> while rendering we ahve to make sure that we keep the new route above
the show route else /new will be conflicted with /:id 
and no id named new would be found, do error

Now we move onto create route
create a post route at /listings, define a new object instance there
with await newListing= new Listing();
then save this in the db and redirect the user to the /listings page


5th : update route
get-> /listings/:id/edit(edit form opens and give option of submit)-> use method override
put-> /istings/:id

6th route: delete route
make the delete route in app.js and add delete form in show.ejs, mkae 
sure you use delete method override in form action


Lecture2: now we will style the website

make public/css/style.css
make layouts/boilerplate.ejs

do npm i ejs-mate
in boilerplate, we have <%- body %>-> this is where our body will be injected
do: 
app.use(express.static(path.join(__dirname, "public")));
const engine = require("ejs-mate");

app.engine("ejs", engine);
app.set("view engine", "ejs");


Navbar: use bootstrap official website code
<% layout("layouts/boilerplate") %>-> write this at the top of each page, after making the navbar in includes folder

we made basic frontend, which you can do easily, we arae using some 
bootstrap classes
sticky-top-> to fix the navbar

Most important thing is how we are procedding while doing decomposition
0- made boilerplate in layouts folder-> this consists of all componenets like navbar and footer and then this is includes in all the .ejs files

1- made includes folder inside views-> consists of navbar,footer etc
2- made public: consists of css folder

Now after eader and footerm we are styling the index.ejs page(which shows all the list)->
pick up the card from bootstrap and modify it, used a google font also
now we will put every card inside an anchor tag so it redirects

navbar aur footer: bootsrap se uthaaye aur modify
lists: initially they were just simple list itmes, we added bootsrap card to each li and put them inside anchor tag

then we updated ui of edit and new.ejs page(very simple)